# PlanIT Frontend

This folder contains the frontend copy (client/ and shared/).

- Install dependencies: `pnpm install` or `npm install`
- Set `VITE_API_URL` in `.env` to point to your backend.
- Start dev: `pnpm dev` or `npm run dev`
